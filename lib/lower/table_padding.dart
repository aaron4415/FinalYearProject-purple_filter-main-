import 'package:flutter/cupertino.dart';

Center wrapper(String text, orange) {
  return Center(
      child: Padding(
          padding: const EdgeInsets.only(top: 4.0, bottom: 4.0),
          child: Text(text)));
}
